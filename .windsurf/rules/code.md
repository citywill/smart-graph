---
trigger: manual
---

# 项目目录结构及文件示例：

- streamlit项目入口为 `app.py`。
- stramlit项目各功能页面存放到 `pages` 目录内。
- 项目菜单在 `app.py` 中使用 `st.navigation` 方法创建，其参数为由 `st.Page` 构建的 list。
- 文件上传路径是uploads。
- 业务逻辑代码写到services中。
- 和业务无关的公共工具类代码写到utils中。
- 对不同的文件类型，要使用专门的文件工具类处理，以便程序的扩展性。
- 提示词集中存放到prompts.py文件中供程序引用。提示词要用中文写作。
- 大模型、数据库相关设置，存放到.env文件中。
- `README.md` 中描述项目标题、功能、目录结构、运行方法等信息。


项目目录结构示例：

```
.
├── app.py
├── pages
    ├── doc_manage.py          //文档管理
    └── chat.py                //智能对话
├── services
    ├── doc_service.py         //文档处理类
    └── rag_service.py         //基于向量召回和知识图谱的问答类
├── utils
    ├── txt_util.py            //文本文件处理工具类
    ├── llm_util.py            //大模型工具类
    └── neo4j_read_util.py     //neo4j读工具类
    └── neo4j_write_util.py    //neo4j写工具类
├── uploads
├── prompts.py
├── .env
├── requirements.txt
└── README.md
```

## 入口文件（`main.py`）示例：

```python
import streamlit as st

# 设置页面配置
st.set_page_config(page_title="项目标题", layout="wide")

# 定义导航页面
pages = [
    st.Page("pages/page1.py", title="页面1标题", icon="📤"),
    st.Page("pages/page2.py", title="页面2标题", icon="📋"),
]

# 创建导航菜单
pg = st.navigation(pages)
pg.run()
```

## 功能页面 （如`pages/page1.py`） 文件示例：

```python
import streamlit as st
import pandas as pd

str = "hello world"

st.write(str)
```

**注意，功能页面的代码不要写到 `main()` 函数中。**

# 注意：

- 项目代码要补充中文注释，详细说明每个页面的功能，以及重要代码段落的作用。